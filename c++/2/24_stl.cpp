#include<iostream>
using namespace std;
//STL-->standard template library
// why use stl? re use: well tested component

// components of stl ----> containers , algorithms              and iterators
//                        (data store)  (sorting &searching )       (object like a pointer)

// containers are of three types
// sequence containers--->linear fashion mai store karte hai data ko(example - vector ,list ,dequeue)
// associatuve containers--->direct access (fast searching) ---> set and multi set , map and multi map
// derived containers--->real world modelling ---> stack,queue, priority -queue

// 

int main(){

return 0;
}